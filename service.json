{
    "kind": "Service",
    "metadata": {
        "labels": {
            "external": "true",
            "{{.NS_SELECTOR}}": "{{.NAME}}"
        },
        "name": "{{.NAME}}"
    },
    "spec": {
        "selector": {
            "{{.NS_SELECTOR}}": "{{.NAME}}"
        },
        "ports": [
            {
                "protocol": "TCP",
                "targetPort": {{.PORT}}
            }
        ]
    }
}
